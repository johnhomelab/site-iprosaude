export const trackPageVisit = async (slug: string) => {
  // Simulating server-side tracking
  console.log(`[Tracking] Page visited: /${slug} at ${new Date().toISOString()}`);

  // Future integration with analytics services can go here
};
